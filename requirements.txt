# Runtime dependencies for Kairos Code.
#
# Hand-kept in sync with [project.dependencies] in pyproject.toml — prefer
# `pip install -e .` (or `.[ollama]`, `.[dev]`) so the packaging metadata stays
# authoritative. This file exists for platforms and pipelines that want a plain
# requirements list.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
pydantic>=2.0
pydantic-settings>=2.0
openai>=1.50.0
anthropic>=0.34.0
httpx>=0.27.0
pyyaml>=6.0
python-dotenv>=1.0.0
